server:
  port: 8082
spring:
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/jeju_stay}
    username: ${DB_USERNAME:jeju_stay}
    password: ${DB_PASSWORD:}
app:
  allowed-origin: ${FRONTEND_ORIGIN:http://localhost:5173}
